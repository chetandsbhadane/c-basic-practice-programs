#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	
	char name[50];
	int add=0;
	
	scanf("%s",name);
	int x = atoi(name);
	
//	if(x % 2 == 0){
//	add = add+x;
	if(name==x){
		printf("\ndsg");
	}
	printf("\n%d",x);
//	}
	return 0;
}
